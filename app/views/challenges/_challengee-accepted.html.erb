<div class="challenge-info text-center text-dark pt-3 pb-5">
  <h2>You have accepted
    <%= link_to @challenger.first_name, user_path(@challenger), class: "btn-link" %><%= @challenger.first_name %>'s challenge.
  </h2>
</div>

<p><%= link_to "Cancel Challenge", decline_challenge_path(@challenge), :method => :patch, :data => {:confirm => "Do your really want to decline the Challenge?"}%></p>
<p><%= link_to "Contact Player", accept_challenge_path(@challenge), :method => :patch, class: "btn btn-primary fixed-bottom" %></p>


<h2 class="waiting-message text-center text-dark pt-3 pb-5">Waiting for an<br> answer from<br><%= link_to @challengee.first_name, user_path(@challengee), class: "btn-link" %> </h2>
